  <header class="topbar">
    <div class="container topbar__inner">
      <a class="brand" href="/dashboard/devices">Kiosk Portal</a>
      <nav class="nav">
        <a href="/dashboard/devices" class="nav__link">Devices</a>
        {{#if user}}
          {{#if (eq user.role 'ADMIN')}}
          <a href="/dashboard/users" class="nav__link">Users</a>
          {{/if}}
        {{/if}}
        <a href="/dashboard/logout" class="nav__link nav__link--danger">Logout</a>
      </nav>
    </div>
  </header>

